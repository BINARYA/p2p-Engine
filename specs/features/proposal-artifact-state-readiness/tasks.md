# Tasks - Proposal Artifact State Readiness

- [x] T001: Review accepted `PROP-086`, steering docs, existing
  `proposal-readiness-review-and-questions` specs, `ProposalArtifactService`,
  readiness, context, validation, CLI, and MCP boundaries; completion is an
  implementation note or code comments only where needed confirming the new
  state lifecycle belongs in a dedicated service. Covers R001-R034, N001-N004.

- [x] T002: Add core artifact state enums/dataclasses in
  `src/p2p_engine/core/proposal_artifact_state.py`; completion is typed models
  for artifact ids, expectations, statuses, confirmation state, risk flags,
  audit/history records, state views, and operation results. Covers R004-R009,
  R012, N005.

- [x] T003: Add `ProposalArtifactStateService` with read/status, initialize,
  set, confirm, mark-legacy, risk-trigger evaluation, validation helpers, and
  atomic write behavior; completion is focused service tests for normal paths
  and E001-E009. Covers R001-R010, R015-R020, R031, R033, N006-N007.

- [x] T004: Wire `P2PWorkspace` facade delegation to the artifact state service
  without adding lifecycle rules to `storage/filesystem.py`; completion is
  facade-level tests or existing service tests proving call shapes remain
  stable. Covers N002-N004.

- [x] T005: Initialize artifact state by default in the proposal creation path;
  completion is a CLI or service test proving a newly created proposal has
  artifact state visible through the public status command. Covers R001-R002,
  R020, AC001.

- [x] T006: Implement advisory legacy behavior for proposals without artifact
  state; completion is tests proving old proposals report `absent_legacy`,
  registry refresh remains valid, and validation does not error. Covers R003,
  R010, R024, R031, E007, E013, AC005.

- [x] T007: Add `p2p proposal artifact ...` CLI command module and register the
  command group; completion is CLI tests for `status`, `init`, `set`,
  `confirm`, `mark-legacy`, output text, invalid values, and missing rationale.
  Covers R021-R024, E001-E006, AC003.

- [x] T008: Add MCP catalog definitions and proposal handler dispatch for
  artifact status/init/set/confirm/mark-legacy; completion is MCP tests proving
  read-only/write-safe descriptions, schemas, payloads, and no governance
  decision side effects. Covers R025-R026, E010-E011, AC004.

- [x] T009: Integrate artifact state into readiness assess/review while keeping
  refresh conservative; completion is readiness tests for each lifecycle state,
  auto-required artifacts, owner-visible cautions, and computed score not being
  replaced by owner judgment. Covers R011-R012, R034, E014, AC006.

- [x] T010: Integrate artifact state into targeted compact context; completion
  is context tests showing missing, weak, unknown, deferred, not_applicable
  rationale, absent_legacy, and suggested next commands without broad scans.
  Covers R013-R014, R032, AC007.

- [x] T011: Implement deterministic risk trigger detection for graduated
  artifact expectations; completion is tests covering governance, policy, CLI,
  MCP, storage, registry, compatibility, migration, permissions, remote sync,
  source-of-truth, agent-memory, docs/install/release, dependencies,
  high-uncertainty, alternatives, and evidence-dependent claim triggers. Covers
  R015-R018, AC008.

- [x] T012: Update validation to inspect present artifact state and report
  actionable findings for malformed state; completion is validation tests for
  valid state, malformed YAML, invalid enum values, missing rationale, and
  absent legacy state. Covers R031, E003-E009, AC005.

- [x] T013: Update generated agent instructions and local/project skills where
  generated by the engine so agents inspect artifact coverage before maturity
  claims, ask one focused question at a time, and use only CLI/MCP write-safe
  primitives for P2P memory mutation; completion is snapshot/content tests.
  Covers R027-R030, AC009.

- [x] T014: Add explicit guard documentation and tests for the no-direct-write
  boundary: no `.p2p` direct edits, no reverse-engineered layouts, and no
  temp-file copy into managed artifacts as an artifact-state workflow;
  completion is tests or documented scenarios proving missing primitives are
  reported instead of bypassed. Covers R028-R030, E012, AC010.

- [x] T015: Review existing `ProposalArtifactService.import_exploration`,
  `import_artifact`, and `import_impact` workflows against the new boundary;
  completion is a design note or follow-up task clarifying whether existing
  import commands remain compatibility behavior and how future large-content
  imports should be exposed through explicit CLI/MCP primitives. Covers R030,
  AC010.

- [x] T016: Update public docs for artifact lifecycle, CLI commands, MCP tools,
  legacy advisory behavior, readiness/context integration, and agent mutation
  rules; completion is docs matching implemented behavior. Covers AC012.

- [x] T017: Add or update registry/context tests only where artifact state is
  exposed to users; completion is tests proving registry refresh remains
  compatible for mixed historical and new proposals. Covers R032, E013, AC011.

- [x] T018: Run focused tests for artifact state service, CLI, MCP, readiness,
  context, validation, and agent instructions; completion is passing focused
  pytest output or documented failures with causes. Covers AC001-AC012.

- [x] T019: Run broad compatibility validation:
  `.venv/bin/p2p validate`, existing readiness/question tests, CLI tests, MCP
  tests, and any touched registry/context tests; completion is reviewed passing
  output or documented residual risk. Covers N001, AC011.

- [x] T020: Before implementation is considered complete, verify task evidence
  from `src/`, `tests/`, `docs/`, or observed CLI/MCP behavior and do not mark
  tasks complete from spec text alone. Covers local dev-spec policy and
  `ENGINEERING_QUALITY_SKILL.md`.

# Tasks - Typed Project Vertical Transition Impact

## Phase 0 - Governance Binding And Contract Freeze

- [x] T001: Bind accepted `PROP-001`, owner decision, readiness, vertical
  coverage, current source evidence and the remaining WaveKit `7.8` gap to
  local requirements, design and tasks. Covers R001-R052.
- [x] T002: Complete a path-level inventory of every current producer,
  consumer, test, fixture, document and generated template that observes
  `VerticalLifecyclePreview`, vertical apply results, loose mapping input or
  vertical mutation receipts; completion is no unclassified reachable
  consumer. Covers R001-R008, R042-R052.
- [x] T003: Capture immutable 0.4.7 characterization fixtures for install,
  empty adoption, populated adoption rejection, migration with mapping,
  implicit orphaning, rubric collision, question reconciliation, apply and
  replay; completion is reviewed evidence of the exact behavior being
  replaced. Covers R009-R027, R036-R044.
- [x] T004: Freeze reviewed JSON schemas and sanitized golden examples for
  `p2p-vertical-transition-impact/v1` and
  `p2p-vertical-transition-plan/v1`, including exact collection limits and
  error versus blocked-preview classification. Covers R001-R008, R027-R033,
  R047-R048, AC012.

## Phase 1 - Typed Domain Contracts

- [x] T005: Add typed operation, evidence-kind, disposition, issue, artifact,
  lock and collection models with strict deterministic serialization; focused
  tests must reject unknown enum states and freeze exact field sets. Covers
  R001-R008, N001, N005.
- [x] T006: Add distinct install, adoption and migration impact models sharing
  only explicit value objects; completion is impossible construction of an
  operation payload missing its required fields. Covers R001-R006, R016-R027.
- [x] T007: Add typed transition-plan and decision models, exact reference
  validation, unique-key YAML loading, canonical ordering and semantic hashes.
  Covers R028-R034, N001.
- [x] T008: Define and enforce per-collection, total-impact and receipt decision
  bounds that fit the current output and receipt hard limits; completion is
  deterministic limit diagnostics with zero writes. Covers R008, R029, N003,
  AC008.
- [x] T009: Add stable transition error/blocker codes and map each to CLI
  blocked-success or failure/exit semantics; completion is a reviewed error
  matrix and focused tests. Covers R027, R037, R044, R047.

## Phase 2 - Authoritative Evidence Classifier

- [x] T010: Implement one immutable transition snapshot and
  `VerticalEvidenceClassifier`; remove lifecycle-local routing predicates after
  all callers use the classifier. Covers R009, R014-R015, N002, N005.
- [x] T011: Classify definition fields, assumptions, blockers and existing
  orphans, including meaningful `0` and `false` values and empty-value edge
  cases. Covers R010, R014-R015, AC001-AC002.
- [x] T012: Integrate current project-question owner-evidence semantics without
  exposing answers; prove answered, applied, deferred, muted and application
  records classify populated while untouched generated questions do not.
  Covers R011, R013-R015, AC002.
- [x] T013: Compare persisted rubrics with the exact active lock baseline and
  classify enablement changes, semantic customization, additions and existing
  rubric orphans; unchanged defaults remain empty evidence. Covers R012-R015,
  AC002.
- [x] T014: Route both adoption and migration mode selection through the same
  classifier and expose bounded evidence-family counts in typed impact. Covers
  R009, R014-R018, AC001-AC002.
- [x] T015: Add focused classifier tests with one evidence family changed per
  case plus mixed evidence, invalid current lock and malformed current artifact
  cases; completion is no dependency on CLI or filesystem presentation.
  Covers R009-R015, R044, N002.

## Phase 3 - Read-Only Transition Analysis

- [x] T016: Implement typed install analysis for exact target/checksum,
  dependency closure, disposition, conflicts and bounded artifact kinds while
  removing install paths/archive names from public impact. Covers R005-R008,
  R016, R027.
- [x] T017: Implement deterministic structural diff for sections and fields,
  including added, removed and changed attributes plus exact compatible
  same-identity detection. Covers R019, R021, N001-N002.
- [x] T018: Analyze every meaningful definition field, assumption, blocker and
  existing orphan into exactly one typed disposition without creating
  candidates. Covers R020-R022, R029-R031, AC003.
- [x] T019: Implement lock, dependency, profile, module and governed-artifact
  semantic impact using domain kinds and hashes rather than filesystem paths.
  Covers R005, R023-R024.
- [x] T020: Extract or extend project-question dry-run reconciliation so
  preserved, revised, created, retired, superseded, inactive-owner-evidence and
  incompatible-answer cases are returned structurally. Covers R025, R027,
  N006, AC006.
- [x] T021: Split rubric comparison from rendering and return structured
  preservation, customization, mapping candidates, orphan candidates and
  semantic collisions. Covers R026-R027, N006, AC006.
- [x] T022: Generate stable required-decision IDs and analysis fingerprints
  from exact semantic identities, then add determinism, ordering, bounds and
  privacy tests. Covers R006-R008, R019-R034, N001-N004.

## Phase 4 - Explicit Plan Validation And Materialization

- [x] T023: Implement strict parsing of the canonical plan supplied through
  `--mapping`, rejecting old loose mapping/list forms, duplicate YAML keys,
  unknown fields, unsupported versions and invalid SHA-256 values. Covers
  R028, R032-R034, R047.
- [x] T024: Validate plan completeness against required decisions, exact source
  identities, compatible target kinds and exclusive target ownership; missing,
  extra, stale and conflicting decisions must remain write-free. Covers
  R029-R035, AC003-AC004.
- [x] T025: Materialize automatic preservation and explicit field,
  assumption/blocker mapping or definition-orphan disposition exactly once
  with provenance retained. Covers R020-R022, R030-R035, AC005.
- [x] T026: Materialize rubric decisions without implicit collision handling;
  preserve compatible customization, map exact criteria and retain explicit
  non-baseline rubric orphans. Covers R026-R035, AC005-AC006.
- [x] T027: Materialize question reconciliation decisions through the existing
  question artifact lifecycle, retaining owner evidence and transitions in
  that memory family. Covers R025, R027, R030-R035, N006, AC005-AC006.
- [x] T028: Refactor project vertical candidate rendering into pure target
  baseline and complete decision-aware candidate construction; remove implicit
  field/rubric orphaning and collision exceptions from the old path. Covers
  R019-R035, N005-N006.
- [x] T029: Validate the complete active vertical, lock, definition, rubrics
  and questions candidate set before a mutation preview can be applicable.
  Covers R035, R037, R041, R044.
- [x] T030: Add service tests proving no loss, duplication or cross-family copy
  for mixed fields, assumptions, blockers, existing orphans, rubrics and owner
  questions. Covers AC003-AC007.

## Phase 5 - Lifecycle, Token, Atomicity And Receipt Integration

- [x] T031: Update lifecycle preview/result types and orchestration to carry
  typed impact, fingerprints, typed semantic postconditions and structured
  issues through a path-free public projection while keeping generic
  mutation-preview paths, per-file physical hashes and token context internal.
  Covers R001-R008, R027, R036-R038.
- [x] T032: Make adoption return the complete empty-project candidate only for
  classifier-approved state and return a typed migration-required blocker for
  populated state. Covers R009-R018, R036-R037, AC001-AC002.
- [x] T033: Make migration execute analysis before materialization, return all
  unresolved decisions in the first preview, and build an applicable preview
  only from a complete current plan. Covers R017-R035, R036-R037, AC003-AC004.
- [x] T034: Bind contract, analysis, plan, source/target, profile/modules and
  candidate semantics into preview token context and prove every material
  change invalidates the token. Covers R038-R040, AC009.
- [x] T035: Recompute classifier, analysis, plan and candidate on apply before
  token comparison; preserve existing confirmation and project-busy behavior.
  Covers R036-R041, R044.
- [x] T036: Advance vertical mutation receipt validation/result to the typed
  current contract, storing bounded decision summary and semantic
  postconditions with internal atomic physical postconditions and a separate
  path-free public status projection. Covers R041-R043, N003-N004.
- [x] T037: Reconstruct the same typed result on exact replay/status and reject
  changed plan, actor, target, token or postcondition state with current stable
  idempotency diagnostics. Covers R042-R044, AC010-AC011.
- [x] T038: Add failure-injection tests before staging, during candidate write,
  after receipt staging and during recovery; completion is old complete state
  or explicit current transaction recovery, never partial migration. Covers
  R036, R041-R044, AC010.

## Phase 6 - CLI And Public Contract

- [x] T039: Update migrate CLI plan loading/help while retaining registered
  command paths, `--mapping` spelling, `--format json` operation IDs and
  argument-vector safety. Covers R028, R045, R047.
- [x] T040: Render concise text classification, counts, decisions, blockers and
  next-step guidance without raw values or internal paths. Covers R007-R008,
  R046, N004.
- [x] T041: Emit exact typed JSON for install/adopt/migrate preview and apply,
  including blocked previews, and enforce exact top-level envelope, safe preview
  summary, path-free mutation result and domain field sets. Covers R001-R008,
  R016-R027, R045-R048.
- [x] T042: Add sanitized golden fixtures for all three operation models,
  incomplete/complete plans, routing blockers, reconciliation conflicts,
  limits, stale plan/token, apply and replay. Covers R047-R048, AC001-AC012.
- [x] T043: Add CLI end-to-end tests that initialize real temporary schema-3
  projects, install exact packs and compare preview claims with post-apply
  definition/question/rubric/lock inspection while asserting no internal path
  or physical hash is public. Covers R036-R048, AC007, AC009-AC012.

## Phase 7 - Agent, MCP And Maintained Documentation Convergence

- [x] T044: Update the agent capability catalog generation/semantics for the
  typed decision workflow while retaining `vertical.project.adoption` as
  owner-governed CLI-only. Covers R050-R051.
- [x] T045: Update generic, Codex and Claude templates/skills to teach
  classification, incomplete preview, exact decision plan, re-preview, token
  replacement, apply and receipt recovery. Covers R050-R051.
- [x] T046: Update MCP catalog/inventory documentation to explain the explicit
  CLI-only lifecycle and verify that no unregistered or accidental mutation
  tool is added. Covers R051, N005-N007.
- [x] T047: Update CLI contract, CLI guide, primitive inventory, agent
  integration guide and changelog with exact schemas, bounds, errors, examples,
  current-only break and downstream pinning guidance. Covers R045-R052.
- [x] T048: Regenerate checked-in agent examples through public agent commands
  and run capability/template command resolution tests in source and wheel
  environments. Covers R050-R051, N007, AC013.

## Phase 8 - WaveKit 7.8 Handoff Verification

- [x] T049: Produce a sanitized fixture manifest from the release-candidate
  wheel declaring engine, global CLI and impact contract versions plus fixture
  checksums. Covers R048, R052, N007, AC014.
- [x] T050: Prove exact empty/populated classification for empty definition,
  definition field, assumption, blocker, existing orphan, owner question and
  custom rubric cases through the installed CLI. Covers R009-R018, AC001-AC002,
  AC014.
- [x] T051: Prove preview completeness for automatic preservation, removed
  sections, field/rubric/question required decisions and a complete mixed
  map/orphan plan, comparing post-apply inspection. Covers R019-R035,
  AC003-AC007, AC014.
- [x] T052: Prove stale analysis, changed decision, stale preview, exact replay,
  changed-input conflict and postcondition drift using stable CLI errors and
  receipt status. Covers R038-R044, AC009-AC011, AC014.
- [x] T053: Prove bounded output and privacy with distinctive secret values,
  maximum collections and one-over-limit cases in source and installed wheel;
  include apply and `mutation status` path/hash redaction while retaining
  internal receipt drift checks. Covers R007-R008, R042-R043, R047-R052,
  N003-N004, AC008, AC014.
- [x] T054: Write the WaveKit handoff note mapping each fixture/field to the two
  remaining `7.8` assertions and explicitly retaining WaveKit ownership of
  authorization, expiry, queue concurrency, crash recovery and post-apply
  validation. Covers R052, AC014.

## Phase 9 - Validation, Release Evidence And Closure

- [x] T055: Run focused model, classifier, analyzer, materializer, question,
  rubric, lifecycle and receipt tests; completion is reviewed exact commands
  and results. Covers AC001-AC011.
- [x] T056: Run `./scripts/test-public.sh` and review CLI, MCP capability,
  generated-template and maintained-document contract results. Covers
  AC012-AC013.
- [x] T057: Build wheel and sdist, run release artifact verification, install
  the wheel in an isolated environment and execute the complete WaveKit handoff
  matrix without source-tree imports. Covers N007-N008, AC014-AC015.
- [x] T058: Run `./scripts/test-full.sh`; any failure or unexplained skip blocks
  release and task completion. Covers N008, AC015.
- [x] T059: Add an implementation note linking source ownership, deleted
  generic paths, contract fixtures, privacy/bounds evidence, receipt evidence,
  agent/MCP convergence, installed-wheel results and the WaveKit handoff to
  accepted `PROP-001`. Covers R001-R052, AC001-AC015.

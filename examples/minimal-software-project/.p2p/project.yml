project:
  id: local-cli-task-tracker
  name: Local CLI Task Tracker
  version: 0.1.0
  status: active
  domain: software
runtime_contract:
  required: true
storage:
  mode: file_based
  documents_format: markdown
  structured_data_format: yaml
workflow:
  current_phase: cli_managed
ai:
  mode: prompt_only
  direct_invocation: false
repository:
  mode: local
  managed_by_p2p: false
remote:
  mode: local
  provider: local
  remote: null
  url: null
  review_request:
    mode: advisory
    opens_external_request: false

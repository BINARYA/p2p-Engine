# Spec Kit Constitution Prompt

Use this content with `/speckit.constitution`. Establish governing principles from accepted P2P memory.

## Principles To Establish

- Preserve accepted project intent and source traceability.
- Do not treat draft P2P proposals as accepted requirements.
- Mark missing information as NEEDS CLARIFICATION.
- Humans supervise outcomes and make governance decisions.
- Agents use P2P exports as structured cognition, not as authority to bypass governance.

## Existing Governance Context

# P2P Engine Constitution

## Purpose

P2P Engine exists to transform collaborative discussion into governed proposals, decisions, execution plans, tasks, and actions.

## Principles

1. The proposal is the central unit of work.
2. AI assists the process but does not make final decisions.
3. Decisions must be explicit, traceable, and reviewable.
4. Git is the source of truth for history, auditability, and portability.
5. GitHub, GitLab, Jira, Linear, OpenSpec, and Spec Kit are downstream integrations, not required foundations.
6. Markdown is used for human-readable artifacts.
7. YAML is used for structured data.
8. The CLI comes before the web app.
9. The MVP must validate the workflow before adding managed AI, MCP, billing, users, permissions, or dashboards.

## Bootstrap Rule

Until P2P Engine exists as a working CLI, the P2P method is applied manually through the `.p2p/` file structure.

## Decision Rules

# Decision Rules

## Default Mode

Bootstrap decisions are made by responsible maintainer decision.

## Decision Outcomes

- `accepted`
- `accepted_with_changes`
- `rejected`
- `deferred`
- `split`
- `merged_into_other`
- `superseded`

## Acceptance Criteria

A proposal can be accepted when:

- the problem is clear;
- the intended outcome is explicit;
- the scope is bounded;
- the implementation plan is actionable;
- risks and exclusions are documented;
- the next execution step is clear.

## AI Constraint

AI-generated content can inform proposal, plan, task, and risk analysis, but the recorded decision must be human-approved.

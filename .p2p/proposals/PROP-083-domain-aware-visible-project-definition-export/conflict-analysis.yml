conflicts:
  - type: compatibility_risk
    proposals:
      - PROP-083
    reason: Existing .p2p/outputs consumers may conflict with a new root-level outputs convention if migration is not explicit.
    suggested_resolution: preserve existing public behavior until implementation inventories dependencies and defines deprecation or mirroring.
  - type: naming_confusion
    proposals:
      - PROP-083
    reason: A root project directory would conflict conceptually with .p2p/project, which is why outputs is preferred.
    suggested_resolution: use outputs as the visible generated output root for the MVP.

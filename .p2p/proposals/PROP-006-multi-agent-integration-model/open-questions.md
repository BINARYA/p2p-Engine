# Open Questions - PROP-006

1. Quali agent supportare nel primo batch oltre Codex: Claude, generic, Gemini, Cursor, Copilot?
2. Il comando deve chiamarsi `p2p agent ...` o `p2p integration ...`?
3. Dove salvare lo stato: `.p2p/agent-integrations.yml` o `.p2p/integrations/agents.yml`?
4. Come tracciare file installati e modifiche manuali?
5. Serve subito un comando `switch/use` oppure basta `install/list/update`?
6. Le skill installate devono essere generate da template interni al package o da template nel progetto?
7. Come distinguere agenti multi-install safe da agenti che condividono file o directory?

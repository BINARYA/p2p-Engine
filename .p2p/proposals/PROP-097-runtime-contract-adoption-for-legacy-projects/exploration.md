# Exploration

Runtime contract adoption is the missing bridge between legacy projects and the
runtime contract model introduced by PROP-084. The command must be intentionally
narrow so it cannot become a hidden installer, recovery workflow, or update
operation.
